# Helpdesk API Service

Backend placeholder for the helpdesk solution (folder: `helpdeskapi`).

Next steps:
- Choose stack (e.g., Node/Express, NestJS, FastAPI, etc.).
- Document setup/run commands once initialized.
- Keep secrets in `.env` (gitignored at root).
