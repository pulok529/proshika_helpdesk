# App Client

Frontend/mobile placeholder for the helpdesk solution.

Next steps:
- Pick framework (React/Next.js/React Native/Flutter, etc.).
- Document install/build/run commands after init.
- Store config in environment files (gitignored).
